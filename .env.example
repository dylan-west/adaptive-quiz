# ---- Database (used by docker-compose) ----
POSTGRES_USER=app_user
POSTGRES_PASSWORD=app_password_dev
POSTGRES_DB=adaptive_quiz
POSTGRES_PORT=5432

# ---- App-side connection URL (backend will read this) ----
DATABASE_URL=postgresql://app_user:app_password_dev@localhost:5432/adaptive_quiz

# ---- Embeddings (adjust later to your chosen model) ----
EMBEDDING_DIM=1536
