# ---- Database (used by docker-compose) ----
POSTGRES_USER=adaptive
POSTGRES_PASSWORD=adaptive
POSTGRES_DB=adaptive_quiz
POSTGRES_PORT=5432

# ---- Backend DB connection (for containers, use hostname 'db') ----
DATABASE_URL=postgresql+psycopg://adaptive:adaptive@db:5432/adaptive_quiz

# ---- OpenAI (enable OCR & item generation) ----
QGEN_PROVIDER=openai
OPENAI_API_KEY=sk-your-key-here
# Optional embedding model override
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small
# Optional: embedding dimension (pgvector column is 1536)
EMBEDDING_DIM=1536

# ---- Frontend ----
NEXT_PUBLIC_API_BASE=http://localhost:8000
